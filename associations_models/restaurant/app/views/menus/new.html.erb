<h1>Add a menu</h1>
<%= form_with scope: :menu, url: cook_waiter_visitor_menu_path, local: true do |form| %>
  <p>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %>
<%= link_to 'Back', cook_waiter_visitor_path(@cook, @waiter, @visitor) %>
<%= link_to 'Cook', cooks_path %>
