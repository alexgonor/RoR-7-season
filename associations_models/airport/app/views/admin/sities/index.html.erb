<h1>Listing cities</h1>
<%= link_to 'New sity', new_admin_flight_plane_country_sity_path %>
<table>
  <tr>
    <th>Name</th>
    <th></th>
  </tr>

  <% @sities.each do |sity| %>
  <tr>
    <td><%= sity.name %></td>
    <td><%= link_to 'Show', admin_flight_plane_country_sity_path(@flight, @plane, @country, sity) %></td>
    <td><%= link_to 'Edit', edit_admin_flight_plane_country_sity_path(@flight, @plane, @country, sity) %></td>
    <td><%= link_to 'Destroy', admin_flight_plane_country_sity_path(@flight, @plane, @country, sity),
            method: :delete,
            data: { confirm: 'Are you sure?' }%></td>
  </tr>
  <% end %>
</table>
<%= link_to 'Root', admin_flights_path %>|
<%= link_to 'Back', admin_flight_plane_countries_path %>
