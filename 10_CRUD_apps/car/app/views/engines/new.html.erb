<%= form_with scope: :engine, url: engines_path, local: true do |form| %>

  <% if @engine.errors.any? %>
      <div id="error_explanation">
        <h2>
          <%= pluralize(@engine.errors.count, "error") %> prohibited
          this engine from being saved:
        </h2>
        <ul>
          <% @engine.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

  <p>
    <%= form.label :capasity %><br>
    <%= form.number_field :capasity %>
  </p>

  <p>
    <%= form.label :power %><br>
    <%= form.number_field :power %>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %>
<%= link_to 'Back', engines_path %>
