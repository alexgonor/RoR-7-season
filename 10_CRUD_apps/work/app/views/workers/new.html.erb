<%= form_with scope: :worker, url: workers_path, local: true do |form| %>

  <% if @worker.errors.any? %>
      <div id="error_explanation">
        <h2>
          <%= pluralize(@worker.errors.count, "error") %> prohibited
          this worker from being saved:
        </h2>
        <ul>
          <% @worker.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

  <p>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </p>

  <p>
    <%= form.label :salary %><br>
    <%= form.number_field :salary %>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %>
<%= link_to 'Back', workers_path %>
