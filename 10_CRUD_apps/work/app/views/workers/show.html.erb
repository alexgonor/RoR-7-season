<p>
  <strong>Name:</strong>
  <%= @worker.name %>
</p>

<p>
  <strong>Salary:</strong>
  <%= @worker.salary %>
</p>
<%= link_to 'Edit', edit_worker_path(@worker) %>
<%= link_to 'Back', workers_path %>
<%= link_to 'Destroy', worker_path(@worker),
        method: :delete,
        data: { confirm: 'Are you sure?' } %>
